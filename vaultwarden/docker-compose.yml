services:
    vaultwarden:
        image: vaultwarden/server:latest
        container_name: vaultwarden
        restart: always
        environment:
            DOMAIN: "https://plougastel.tailb3be33.ts.net"
            SIGNUPS_ALLOWED: "true"
        volumes:
            # - ./:/ssl/  <-- REMOVE OR COMMENT OUT THIS LINE
            - ./vw-data:/data
        ports:
            - "11001:80"
