services:
  caddy:
    image: caddy:2
    container_name: caddy
    restart: unless-stopped
    network_mode: host            # voit 127.0.0.1:11001 et :5006 et écoute sur :8000
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
